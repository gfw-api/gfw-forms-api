prod:
  image: vizzuality/gfw-forms-api:1.0.0${STAGING}
  ports:
    - "4400"
  env_file:
    - prod.env
  command: start
  dns: ${DNS_SERVER}
  restart: always
  log_driver: "gelf"
  log_opt:
    gelf-address: ${GELF_ADDRESS}
